//opening up to the opening screen when app first opens

    //putting the intro card onto the page with listener
    makeIntroCard();

    function gameStart() {
        makeGameChoiceCard();
    };

